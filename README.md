# Dual-Port-RAM
Dual Port RAM (DPRAM) is a type of memory that allows two independent ports to access the same memory location simultaneously. Unlike single-port RAM, it enables parallel read and/or write operations, improving system speed and efficiency.
